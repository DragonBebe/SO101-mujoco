# 2026-09-21 GitHub 完整保存快照

分支：`realsim-sync-and-real-stack-20260921`。

包含工作区现有代码修改、realsim模块和文档、场景与标定、完整真机日志目录（所有历史相机帧、actions.jsonl、原始反馈及转换日志）、规范堆叠回位归档，以及此前被忽略的全部 LLM-control/runs 历史运行文件。虚拟环境、Python缓存和测试缓存不属于保存内容。

两份连续反馈JSONL超过普通Git单文件限制，使用Git LFS保存原文，未抽样、压缩或删减。完整清单及LFS SHA256、记录数、截止时间见 `2026-09-21-upload-snapshot.json`。运行服务没有停止；暂存之后产生的实时反馈和live相机文件变化不属于该固定快照。

拉取全部内容：

```bash
git clone --branch realsim-sync-and-real-stack-20260921 https://github.com/DragonBebe/SO101-mujoco.git
cd SO101-mujoco
git lfs install
git lfs pull
```

使用GitHub网页下载ZIP不保证包含LFS原始大文件，请使用上述克隆方式。

本次上传前重新验证MuJoCo环境：182 passed、6 skipped。原始导出XML中存在行尾空格，为保持归档不变未清理；这不是测试失败。相机环境重新验证：116 passed、5 skipped。
