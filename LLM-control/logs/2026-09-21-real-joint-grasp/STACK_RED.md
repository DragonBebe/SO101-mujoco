# 红块跨放绿、蓝块：真机执行记录

2026-09-21，用户明确要求红块叠放在相邻绿、蓝两块上面。使用当前对话逐步规划，真机执行有界关节增量和夹爪指令；每步读取编码器并检查环境、腕部相机。没有使用 IK 或动作回放，没有重新采集标定。

## 结果与证据

视觉判断：成功。红块跨在绿、蓝两块接缝上方，两底块仍相邻。第 126 帧下降后红块姿态变平正；127、128 帧逐步张开夹爪；129、130 帧抬离。第 131 帧重新观察，距第 128 帧释放约 42.4 秒，红块保持稳定，夹爪已离开，无人工接触方块。此结论是两路 RGB 图像的视觉判断，未测量接触压力或毫米级放置误差。

- [最终环境图](0131-env.png)
- [最终腕部图](0131-wrist.png)
- 完整指令、实测编码器和逐帧图像索引：`actions.jsonl`
- 原始连续反馈：`raw-feedback.jsonl`
- MuJoCo 转换日志：`ee-mapping-fix/live-converted.jsonl`

## 执行过程中的修正

首次闭爪推动并旋转了红块，重新张开和调整接近位置。一次试提未确认离桌，下降重新夹紧后，在第 105 帧起确认提起。搬运时红块略倾斜，逐步平移、收回，最终小幅下降使底面就位后松爪。没有将失败尝试记为成功，也没有用仿真物体瞬移制造结果。

## 结束状态与映射限制

最后实测电机 ID 1..6 编码器：`{'1': 2003, '2': 1829, '3': 2471, '4': 1992, '5': 2151, '6': 2359}`。机械臂保持抬离姿态，夹爪张开，控制服务、相机和 MuJoCo 同步服务保留运行；没有关闭扭矩，也没有另行回零。

结束检查 MuJoCo 状态 `live`，累计接收约 29.9 Hz、刷新约 59.7 Hz；这不是动作到屏幕的端到端延迟。机器人关节持续映射，但导出场景中的物体位置仍是静态初始化；真实红块运动没有同步进场景。仿真夹爪开合仍标记 `uncalibrated`。腕转零点修正为视觉估计，详见 [映射修复记录](ee-mapping-fix/README.md)。控制日志旧 `degrees.wrist_roll` / 辅助 FK 与修正后显示约定不同，勿当作标定验证。
