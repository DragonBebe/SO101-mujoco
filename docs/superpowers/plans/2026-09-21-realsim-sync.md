# realsim 反馈镜像实施记录

目标与规格：按本轮用户要求，先核查并更新现有中文 README，再增加实际关节反馈同步；保留未提交场景、资产和抓取流程。直接在当前工作区实现，不提交用户实验。

架构：已有 ServoBus 所有者可选启用后台只读采样，共用事务锁，原子写最新状态 JSON；独立采集仅在设备空闲时打开。仿真单次加载场景 XML，以具名 qpos + mj_forward 镜像；原始 JSONL 及转换日志支持离线回放。无 IK，无运动指令，无每帧 XML 导出。

- [x] 先更新 REALSIM_README：区分离线导出、连续物体观测、既有一次性关节读取，记录标定缺口。
- [x] 新增反馈协议、发布器与 ServoBus 可选集成；测试异常、串口互斥与退出无写入。
- [x] 新增显式关节映射，复用场景关联的手眼标定；夹爪无物理标定时保持 unknown，支持有来源的两点角度标定。
- [x] 新增 sync / arm-stream / arm-replay，单模型刷新、时间戳、有效性、超时与最新值策略、日志。
- [x] 对齐机器人 baseframe 与桌面系，导出及同步共用变换；测试具名地址、夹爪、范围、时间倒退、掉线恢复。
- [x] 运行两个环境的 realsim 测试和既有回归，设备可用时只读采样；更新实际命令和实测/合成区分。

关注：控制线程与后台 SDK 事务不可交错；异常不归零；缺失/过期/非有限值不得应用；夹爪不能沿用未核实 fraction；重播时间必须保留原采样时间但使用回放时钟判定；坐标变换不可重复应用；拒绝非协作串口持有者。软件延迟不称为运动到画面的端到端延迟。

结果：真机节点可打开但未收到状态包；只完成错误/退出实机路径，真实运动验收留待现场。夹爪缺少物理角度标定，按用户要求保持未标定而不猜参数。原场景 XML 没有覆盖，本轮导出和合成证据放在新的日志目录。
